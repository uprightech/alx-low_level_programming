#include "main.h"

/**
* mul - multiplies two integers
* @a: is first number to be multiplied
* @b: is second number to be multiplied
*
* Return: the result of the multiplication
*/

int mul(int a, int b)
{
	return (a * b);
}
